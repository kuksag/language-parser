abc (cde)
[abc (cde) && def (ghi)]
[abc (cde) || def (ghi)]
[abc -> cde]
[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]
qwe () {abc (cde)}
qwe (rty) {[abc (cde) && def (ghi)]}
[[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]]
[[[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]] || [[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]]]
verylong (input) {[[[[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]] || [[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]]] && [[[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]] || [[[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]] && [[abc (cde) && def (ghi)] && [abc (cde) || def (ghi)]]]]]}
